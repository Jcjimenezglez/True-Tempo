# SEO Setup Guide - Superfocus

Domain: https://www.superfocus.live

## Keyword strategy

**Primary demand source:** Google Ads Keyword Planner seeded on Pomofocus (market leader), not GSC-only queries.

- Chase head terms Pomofocus already owns: `pomodoro timer`, `pomodoro timer online`, `pomodoro technique timer`, `study timer`, `focus timer`, `best pomodoro app/timer`.
- GSC validates what we already rank for; Planner sets the target market.
- Copy pattern (homepage + priority spokes): product/keyword-first titles and H1s, Pomofocus-shaped What is / How to use structure, Superfocus differentiators in Features.

**Priority URLs (request indexing after deploy):**
- `/` (homepage)
- `/techniques/pomodoro-technique`
- `/use-cases/study-timer`
- `/use-cases/focus-timer`
- `/alternatives/pomofocus`
- `/alternatives/best-pomodoro-apps`
- `/compare/superfocus-vs-pomofocus`

## Completed SEO Optimizations

### Meta Tags & HTML Structure
- Planner/Pomofocus-aligned titles via homepage + `scripts/apply-pain-copy.js` (~103 pSEO landing pages)
- Optional per-page `keywords` field in pSEO JSON (falls back to `keyword, pomodoro timer, focus timer, Superfocus`)
- Open Graph + Twitter Cards (`og:site_name`, `twitter:site`)
- Canonical URLs on all indexable pages (`www.superfocus.live`)
- Google Search Console verification placeholder in `index.html`

### pSEO & Hub Pages
- **103 spoke pages** across techniques, use-cases, compare, alternatives, faq, etc.
- **4 hub pages** (category indexes): `/techniques/`, `/use-cases/`, `/compare/`, `/alternatives/`
- Internal linking: `related` fields in JSON + shared footer on pSEO template and homepage
- High-intent pages: `/compare/superfocus-vs-pomofocus`, `/alternatives/pomofocus`, `/alternatives/best-pomodoro-apps`, `/alternatives/hustly-focus`

### Structured Data (JSON-LD)
- Homepage: Organization, SoftwareApplication, HowTo, FAQPage
- pSEO pages: dynamic WebPage, BreadcrumbList, category-specific FAQPage / HowTo / SoftwareApplication
- Hub pages: CollectionPage
- Pricing: SoftwareApplication with Offer

### Technical SEO Files
- `sitemap.xml` — auto-regenerated by `npm run build:pseo` (~110 URLs including hubs)
- `robots.txt` — allows crawlers, blocks `/api/`
- `llms.txt` — AI crawler summary at site root
- `.well-known/security.txt` — security contact
- Redirects in `vercel.json`: `superfocus.live` → `www`, `/focus-timer` → `/use-cases/focus-timer`

### Performance
- pSEO pages use lightweight `pseo/template.html` (no full app shell)
- `npm run build:assets` — esbuild minify to `/dist/` with immutable cache headers
- Deferred analytics via `analytics-deferred.js`
- Core Web Vitals reporting via web-vitals → GA4

## Build Commands

```bash
npm run build          # apply pain copy + minify assets + regenerate pSEO pages + hubs
npm run build:assets   # minify JS/CSS to dist/
npm run build:pseo     # regenerate landing pages, hub indexes, and sitemap
npm run apply-pain-copy # update copy fields in pseo JSON sources from COPY map
```

**Workflow after copy changes:**
1. Edit titles/descriptions in `scripts/apply-pain-copy.js` (or add slugs to `pseo/pages.json` / `pseo/databases/`)
2. Run `npm run build`
3. Deploy via `git push` (Vercel auto-deploys)

## Required Manual Setup (Post-Deploy)

### Google Search Console
1. Go to [Google Search Console](https://search.google.com/search-console/)
2. Use property: `https://www.superfocus.live` (preferred canonical)
3. Copy verification meta tag into `index.html` (uncomment line ~63)
4. Submit sitemap: `https://www.superfocus.live/sitemap.xml`
5. Request indexing for priority URLs:
   - `/` (homepage — pomodoro timer online)
   - `/techniques/pomodoro-technique`
   - `/use-cases/focus-timer`
   - `/use-cases/study-timer`
   - `/compare/superfocus-vs-pomofocus`
   - `/alternatives/pomofocus`
   - `/alternatives/best-pomodoro-apps`
   - `/alternatives/hustly-focus`
6. Confirm `superfocus.live` (non-www) impressions decline over 4–6 weeks (301 redirect is configured)

### Google Analytics
- Measurement ID `G-T3T0PES8C0` configured (loaded via deferred analytics)

### OG Image
- Generate with `npm run update-logo` or ensure `og-image.png` exists at site root (1200×630)

## Monitoring & KPIs (90-day targets)

Review GSC every 2 weeks. Focus on **revenue signal**, not vanity clicks.

| Metric | Baseline (Jun 2026) | 90-day target |
|--------|---------------------|---------------|
| Clicks/month | ~80 | 300+ |
| Pages with clicks | ~8 | 30+ |
| Non-brand CTR | ~2% | 5%+ |
| Position `pomofocus` | ~30 | <15 |

### GSC checks
- **Queries:** head terms (`pomodoro timer`, `pomodoro timer online`, `study timer`, `focus timer`, `pomofocus`, `best pomodoro app`)
- **Pages:** CTR on `/`, `/techniques/pomodoro-technique`, `/compare/`, `/alternatives/`, `/use-cases/`
- **Countries:** Vietnam (strong CTR) vs US (high impressions, low CTR)

### GA4 segments
- Landing page starts with `/compare/`, `/alternatives/`, or `/use-cases/`
- Session source = google / organic

### Mixpanel / product funnel
- CTAs use `?ref=pseo-{slug}` — track signup and paid conversion by slug
- Compare `ref=pseo-superfocus-vs-pomofocus` vs `ref=pseo-hustly-focus` vs `ref=pseo-study-timer`

### PageSpeed
- Test homepage + 3 pSEO URLs after each deploy

## Priority Keyword Clusters

1. **Pomodoro / focus timer:** `/techniques/pomodoro-technique`, `/faq/pomodoro-timer-online`, `/use-cases/focus-timer`
2. **Study focus:** `/use-cases/study-timer`, `/use-cases/focus-website-for-studying`
3. **Competitors (bottom funnel):** `/compare/superfocus-vs-pomofocus`, `/alternatives/pomofocus`, `/alternatives/hustly-focus`

## Not in automated build (future)

- Long-form blog via Outrank
- Backlink network / guest posts
- Localized pages (e.g. Vietnamese for SEA traffic)
